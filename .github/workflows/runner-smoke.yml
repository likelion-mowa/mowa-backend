name: Runner Smoke Test

on:
  workflow_dispatch:

jobs:
  smoke-test:
    runs-on: [self-hosted, linux, x64]

    steps:
      - name: Check runner
        run: |
          echo "Self-hosted runner is working"
          whoami
          uname -m
          java -version
          pwd